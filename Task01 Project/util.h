#include <iostream>
#include <string>
#define N 4 
#define M 6
using namespace std;

string convert(int matrix[N][M]);
void rand_init(int matrix[N][M], int a, int b);
void user_init(int matrix[N][M]);
